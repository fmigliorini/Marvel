import styled from "styled-components";

const SearchInput = styled.input`
  display: flex;
  max-width: 100%;

  padding: 10px;

  border: 1px solid;
  border-radius: 10px;
  border-color: black;

  color: black;
  font-size: 1rem;
`;

export default SearchInput;
