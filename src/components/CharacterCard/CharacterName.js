import styled from "styled-components";

const CharacterName = styled.span`
  align-self: flex-end;

  padding: 20px;

  font-size: 1em;
  font-weight: 700;
  color: black;
`;

export default CharacterName;
